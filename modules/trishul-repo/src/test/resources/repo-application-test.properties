# DB encryption placeholders
db.encryption.algorithm=AES/ECB/PKCS5Padding
db.encryption.key=fake-encryption-key-for-test

# Override JPA database for testing
spring.jpa.database=HSQL
spring.jpa.database-platform=org.hibernate.dialect.HSQLDialect