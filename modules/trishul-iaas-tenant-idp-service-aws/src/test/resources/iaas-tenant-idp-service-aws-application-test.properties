# AWS Cognito User Service
aws.cognito.region=fake-region
aws.cognito.url=https://fake-cognito-url/
aws.cognito.user-pool.id=fake-user-pool-id
aws.cognito.user-pool.url=https://fake-user-pool-url/
# Set access key and secret key if different aws user is used for cognito service
# Otherwise it will pick either values set for aws.access-key and aws.access-secret or
# credentials of aws iam role assigned for EC2
aws.cognito.access-key=fake-cognito-access-key
aws.cognito.access-secret=fake-cognito-access-secret

# AWS Cognito Identity Pool
aws.cognito.identity.pool.id=fake-identity-pool-id
